<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- IMEI Section -->
        <TextView
            android:id="@+id/txtImeiHint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Where can I find the IMEI?"
            android:textSize="16sp"
            android:layout_marginBottom="8dp" />

        <EditText
            android:id="@+id/edtIMEI"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Paste IMEI here"
            android:inputType="text"
            android:longClickable="true"
            android:focusableInTouchMode="true" />

        <!-- MAC Section -->
        <TextView
            android:id="@+id/txtMacHint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Where can I find the MAC address?"
            android:textSize="16sp"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="8dp" />

        <EditText
            android:id="@+id/edtMAC"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Paste MAC address here"
            android:inputType="text"
            android:longClickable="true"
            android:focusableInTouchMode="true" />

        <Button
            android:id="@+id/btnSubmit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Submit"
            android:layout_marginTop="32dp" />

    </LinearLayout>
</ScrollView>
